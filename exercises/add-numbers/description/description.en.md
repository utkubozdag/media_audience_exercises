# Add Two Numbers

Write a function called `add_numbers` that takes two numeric arguments and returns their sum.

## Example

```r
add_numbers(3, 5)
# Returns: 8

add_numbers(10, -2)
# Returns: 8

add_numbers(0, 0)
# Returns: 0
```

## Function Signature

```r
add_numbers <- function(a, b) {
  # Your code here
}
```
